import { HeaderContainer, HeaderContent, Title } from './Header.styles';

const Header = () => (
  <HeaderContainer>
    <HeaderContent>
      <Title>Coffee Shops</Title>
    </HeaderContent>
  </HeaderContainer>
);

export default Header;
